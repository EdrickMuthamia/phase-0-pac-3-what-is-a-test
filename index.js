const name = "Joe";
const height = 74;
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
console.log (message)
/*
describe('what-is-a-test', () =
  describe('Name', () => {
    it('returns "Susan"', () =>
      expect(name).toEqual('Sus
    })
  })


  describe('Height', () => {
    it('is less than 40', () =>
      expect(height).toBeLessTh
    })
  })

  describe('Message', () => {
    it('gives the name and heig
      expect(message).toInclude
      expect(message).toInclude
    })
  })
})
*/